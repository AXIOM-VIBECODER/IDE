[phases.setup]
nixPkgs = ["nodejs_20", "git", "python3"]

[phases.install]
cmds = ["npm install --production --ignore-scripts"]

[phases.build]
cmds = ["echo 'No build step — Node.js web server'"]

[start]
cmd = "node src/server.js"
